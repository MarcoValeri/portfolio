import './title-elastic.scss';

const TitleElastic = ({ title }) => {

    return (
        <div className='title-elastic'>
            <h2 className='title-elastic__title'>{ title }</h2>
        </div>
    )

}

export default TitleElastic;
